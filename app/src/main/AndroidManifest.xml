<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.picturesapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Picturesapp">
        <activity android:name=".shoess"></activity>
        <activity android:name=".Shoee" />
        <activity android:name=".shoe4" />
        <activity android:name=".Shoe4" />
        <activity android:name=".shoe3" />
        <activity android:name=".shoe1" />
        <activity android:name=".Shoes" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>